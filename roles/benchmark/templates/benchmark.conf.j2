[program:muta_benchmark]
command=node index -d duration -g gap -c connections --cpu cpuct --json benchmark_nodes
directory=benchmark_dir
numprocs=1
autostart=true
autorestart=true
startretries=10
exitcodes=0
stopasgroup=true
killasgroup=true
stopsignal=KILL
stopwaitsecs=10
redirect_stderr=true
